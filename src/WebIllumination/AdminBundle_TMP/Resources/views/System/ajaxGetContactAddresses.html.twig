<div id="contact-address-success-message-{{ contactId }}" class="ui-widget message closeable ui-helper-hidden">
    <div class="ui-state-success ui-corner-all">
    	<span class="ui-icon ui-icon-circle-check"></span>
        <p><strong>SUCCESS:</strong> <span id="contact-address-success-message-text-{{ contactId }}"></span></p>
    </div>
</div>
<div id="contact-address-error-message-{{ contactId }}" class="ui-widget message closeable ui-helper-hidden">
    <div class="ui-state-error ui-corner-all">
    	<span class="ui-icon ui-icon-alert"></span>
        <p><strong>ERROR:</strong> <span id="contact-address-error-message-text-{{ contactId }}"></span></p>
    </div>
</div>
<div id="contact-address-confirm-delete-{{ contactId }}" class="ui-widget message ui-helper-hidden">
    <div class="ui-state-error ui-corner-all"> 
        <span class="ui-icon ui-icon-help"></span>
        <p><strong>WARNING:</strong> Are you sure you want to delete the highlighted contact address?</p>
        <p>
        	<button data-contact-id="{{ contactId }}" data-index="" id="contact-address-confirm-delete-button-{{ contactId }}" class="button ui-button-red action-delete-contact-address" data-icon-primary="ui-icon-closethick">Confirm Delete</button>
        	<button data-contact-id="{{ contactId }}" data-index="" id="contact-address-cancel-delete-button-{{ contactId }}" class="button ui-button-blue action-cancel-delete-contact-address">Cancel</button>
        </p>
    </div>
</div>
<div id="contact-address-confirm-deletes-{{ contactId }}" class="ui-widget message ui-helper-hidden">
    <div class="ui-state-error ui-corner-all">
    	<span class="ui-icon ui-icon-help"></span>
        <p><strong>WARNING:</strong> Are you sure you want to delete the selected contact addresses?</p>
        <p>
        	<button data-contact-id="{{ contactId }}" id="contact-address-confirm-deletes-button-{{ contactId }}" class="button ui-button-red action-delete-contact-addresses" data-icon-primary="ui-icon-closethick">Confirm Delete</button>
        	<button data-contact-id="{{ contactId }}" id="contact-address-cancel-deletes-button-{{ contactId }}" class="button ui-button-blue action-cancel-delete-contact-addresses">Cancel</button>
        </p>
    </div>
</div>
<div class="clearfix">
    <div class="form-input-long">
		<div class="contact-addresses-container">
			<input type="hidden" id="contact-address-count-{{ contactId }}" value="{% if contactAddresses|length > 1 %}{{ contactAddresses|length }}{% else %}1{% endif %}" />
			<ul class="list-fields contact-addresses-container" id="form-contact-addresses-container-{{ contactId }}">
				{% for contactAddress in contactAddresses %}
					{% include 'WebIlluminationAdminBundle:System:contactAddress.html.twig' with {'contactId': contactId, 'index': loop.index, 'data': contactAddress, 'contactAddressTypes': contactAddressTypes, 'contactTitles': contactTitles} %}
			    {% else %}
			    	{% include 'WebIlluminationAdminBundle:System:contactAddress.html.twig' with {'contactId': contactId, 'index': "1", 'contactAddressTypes': contactAddressTypes, 'contactTitles': contactTitles} %}
				{% endfor %}
			</ul>
		</div>
	</div>
</div>
<div class="clearfix">
    <div class="form-input-long buttons no-margin-bottom">
    	<button data-contact-id="{{ contactId }}" class="button ui-button-green action-update-contact-addresses" data-icon-primary="ui-icon-check">Update</button>
    	<button data-contact-id="{{ contactId }}" class="button ui-button-red action-confirm-delete-contact-addresses ui-helper-hidden" data-icon-primary="ui-icon-closethick">Delete</button>
    	<button data-contact-id="{{ contactId }}" class="button ui-button-blue action-add-new-contact-address" data-icon-primary="ui-icon-plusthick">Add</button>
    	<button data-contact-id="{{ contactId }}" class="button ui-button-blue action-select-all-contact-addresses" data-icon-primary="ui-icon-radio-on">Select All</button>
    	<button data-contact-id="{{ contactId }}" class="button ui-button-blue action-unselect-all-contact-addresses ui-helper-hidden" data-icon-primary="ui-icon-bullet">Unselect All</button>
    </div>
</div>