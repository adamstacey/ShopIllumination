<h3>Documents</h3>
<table width="100%" cellpadding="0" cellspacing="0" border="0">
	<tr>
		<td class="label">Order printed?</td>
		<td class="gradient-background-{% if data.item.orderPrinted > 0 %}green{% else %}red{% endif %}">{{ (data.item.orderPrinted > 0?'YES':'NO') }}</td>
	</tr>
	<tr>
		<td class="label">Delivery note printed?</td>
		<td class="gradient-background-{% if data.item.deliveryNotePrinted > 0 %}green{% else %}orange{% endif %}">{{ (data.item.deliveryNotePrinted > 0?'YES':'NO') }}</td>
	</tr>
</table>