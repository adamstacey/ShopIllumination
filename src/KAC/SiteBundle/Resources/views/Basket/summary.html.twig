<style>
    .basket-summary-header {
        height: 50px;
        width: 230px;
        margin: 0;
        padding: 5px 10px;

        position: absolute;
        right: 0;
        bottom: -5px;

        border-radius: 4px 4px 0 0;
        background: -webkit-linear-gradient(top, white 0%, #c5d2d6 100%);
        -webkit-box-shadow: 0 -2px 5px 0 #384246;
        cursor: pointer;
    }
    .basket-summary-header h2 {
        margin: 0;
        padding: 0;

        font-size: 1.4rem;
        color: #384246;
        line-height: 24px;
        letter-spacing: 1px;
        text-shadow: 0 1px 0 white;
    }
    .basket-summary-header p {
        margin: 0;
        padding: 0;

        line-height: 13px;
        font-size: 1rem;
        color: #6c8087;
    }
    .basket-summary-products {
        height: 50px;
        width: 230px;
        margin: 0;
        padding: 5px 10px;

        position: absolute;
        right: 0;
        top: 60px;

        border-radius: 0 0 4px 4px;
        background: -webkit-linear-gradient(top, white 0%, #c5d2d6 100%);
        -webkit-box-shadow: 0 2px 5px 0 #384246;
        cursor: pointer;
    }
</style>
<div class="basket-summary-header">
    <a href="#"><span class="fr ui-button-icon-primary ui-icon-small icon-small-grey icon-small-252"></span></a>
    <h2>Your basket</h2>
    <p>Items: <strong>{{ basket.totals.items }}</strong></p>
    <p>Total: <strong>&pound;{{ basket.totals.subTotal|number_format(2) }}</strong></p>
</div>
<div class="basket-summary-products">

</div>
{#<a title="View your basket" href="{#{{ path('basket_your_basket') }}"><img class="fr" src="{{ asset('/bundles/kacshop/images/icons/shopping-basket.png') }}" width="39" height="38" border="0" alt="Your Shopping Basket" /></a>
<h2>Your basket</h2>
<div id="basket-summary-content">
    <p class="fl">Items: <strong>{{ basket.totals.items }}</strong><br />Total: <strong>&pound;{{ basket.totals.subTotal|number_format(2) }}</strong></p>
    {% if basket.totals.items > 0 %}
        {% if basket.delivery.service != '' %}<a title="Go to checkout" href="{{ path('checkout_secure_checkout') }}" class="button ui-button-green">Pay Now</a>{% endif %}
        <a title="View your basket" href="{{ path('basket_your_basket') }}" class="button ui-button-blue">View</a>
    {% endif %}
</div>#}