<div class="ui-widget mbl">
    <header class="ui-widget-header ui-corner-top">
        <h2>Steps{{ flow.getCurrentStepNumber() }}</h2>
    </header>
    <section class="ui-widget-content ui-corner-bottom">
        <nav class="left-menu">
            <ul class="clearfix">
                {% for stepLabel in flow.getStepLabels() %}
                    {% if loop.index == flow.getCurrentStepNumber() %}
                        {% set statusIcon = 'icon-471' %}
                        {% set statusIconColour = 'icon-white' %}
                        {% set buttonColour = ' button-turquoise' %}
                        {% set iconColour = 'turquoise' %}
                    {% else %}
                        {% if flow.isStepDone(loop.index) or flow.isStepDone(loop.index-1) %}
                            {% set statusIcon = 'icon-1119' %}
                            {% set statusIconColour = 'icon-white' %}
                            {% set buttonColour = ' button-green' %}
                            {% set iconColour = 'green' %}
                        {% else %}
                            {% set statusIcon = 'icon-1116' %}
                            {% set statusIconColour = 'icon-red' %}
                            {% set buttonColour = '' %}
                            {% set iconColour = 'grey' %}
                        {% endif %}
                    {% endif %}

                    <li class="clearfix {{ iconColour }}{% if loop.last %} mbn{% endif %}">
                        {% if loop.index == 1 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-946"></span>
                        {% elseif loop.index == 2 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-398"></span>
                        {% elseif loop.index == 3 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-700"></span>
                        {% elseif loop.index == 4 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-874"></span>
                        {% elseif loop.index == 5 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-277"></span>
                        {% elseif loop.index == 6 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-69"></span>
                        {% elseif loop.index == 7 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-944"></span>
                        {% elseif loop.index == 8 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-369"></span>
                        {% elseif loop.index == 9 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-1103"></span>
                        {% elseif loop.index == 10 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-1305"></span>
                        {% elseif loop.index == 11 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-51"></span>
                        {% elseif loop.index == 12 %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-1249"></span>
                        {% else %}
                            <span class="nav-icon ui-button-icon-primary ui-icon icon-{{ iconColour }} icon-946"></span>
                        {% endif %}

                        {% if flow.isAllowDynamicStepNavigation() and (loop.index != flow.getCurrentStepNumber()) and (flow.isStepDone(loop.index) or flow.isStepDone(loop.index-1)) and (not flow.isStepSkipped(loop.index)) %}
                            <a data-icon-secondary="{{ statusIcon }}" class="fl ui-button button{{ buttonColour }} {{ statusIconColour }}" href="{{ path(app.request.attributes.get('_route'), app.request.query.all | merge(app.request.attributes.get('_route_params')) | craue_addDynamicStepNavigationParameter(flow, loop.index)) }}">
                                {{ stepLabel|raw }}
                            </a>
                        {% else %}
                            <button data-icon-secondary="{{ statusIcon }}" class="fl ui-button button{{ buttonColour }} {{ statusIconColour }}">
                                {{ stepLabel|raw }}
                            </button>
                        {% endif %}
                    </li>
                {% endfor %}
            </ul>
        </nav>
    </section>
</div>
